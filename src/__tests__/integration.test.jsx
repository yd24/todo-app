'use strict';

import '@testing-library/jest-dom';
import { render, screen } from '@testing-library/react';
import App from '../App';

describe('Check that components are properly rendering', () => {
  render(<App />);
  
  test('Header is displaying', () => {

  });

  test('To-do form is displaying', () => {

  });

  test('List is displaying', () => {

  });

  test('Footer is displaying', () => {

  });
});